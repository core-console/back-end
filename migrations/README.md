# Migrations

The first real revision, `20260802_01`, creates the persisted users schema and
its database constraints. Migrations create schema only: they do not seed users,
create an administrator, or run automatically during application startup.
